package com.br.lp3.controller;


public class LoginManager {
    
   
    public static boolean authorize(String username, String password){
        return ("giovanni".equals(username) && "a".equals(password));       
    }
   
    
        
}
